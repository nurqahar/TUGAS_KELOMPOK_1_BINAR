<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders</title>
</head>
<body>
    <h1>Orders</h1>
    <ul>
        <% orders.forEach(order => { %>
            <li>ID: <%= order.id %>, Menu: <%= order.menuItems %></li>
        <% }) %>
    </ul>
    <a href="/">Back to Home</a>
</body>
</html>
